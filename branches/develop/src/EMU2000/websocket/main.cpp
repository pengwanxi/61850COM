#include <iostream>
#include <stdio.h>
#include "../share/UdpPort.h"
#include "../share/global.h"
#include <pthread.h>
#include <sys/select.h>
#include <unistd.h>

#include "WebSocket.h"
#include <pthread.h>


using namespace std;

//void *pthread_data(void *arg)
//{
//	CWebSocket wk;
//}
int main()
{

	//pthread_t websocket_id;
	//int pthreadret;
	//int i = 0;
	//
	//pthreadret = pthread_create(&websocket_id, NULL, pthread_websocket, NULL);
	//if (pthreadret != 0)
	//	printf("pthread_DataDevcheck creat error!");
	//pthread_join(websocket_id, NULL);
	//return 0;
	CWebSocket wk;
	return 0;
}